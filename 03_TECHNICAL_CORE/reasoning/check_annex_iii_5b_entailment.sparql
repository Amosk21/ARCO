PREFIX : <https://arco.ai/ontology/core#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

# Audit check: confirms the reasoner inferred Annex III 5(b) applicability.
# Classification itself is an OWL entailment — this just verifies it materialized.
# Should be FALSE before reasoning, TRUE after.
# Positive path only: fraud-detection exclusion deferred.

ASK WHERE {
  :CreditScore_AI_System rdf:type :AnnexIII5bApplicableSystem .
}
